import { Config } from "@business-as-code/core";

export const config = {
  projectSource: [{
    provider: 'core',
    options: {
      // l: 'l',
    }
    // getProject: {
    //   origin: {

    //   }
    // }
  }],
} satisfies Config