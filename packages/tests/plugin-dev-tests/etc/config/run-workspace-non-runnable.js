// @ts-check
/**
 * @typedef {import('@business-as-code/cli').ConfigRun} Config
 * @type {Config}
 */
const config = {
  query: 'projectType=library || projectType=application',
  command: ''
}

module.exports.config = config;
